---
/**
 * TrustBar — Displays site-wide trust signals and key credentials.
 * Include at the top of any page that benefits from instant authority signals.
 */
const credentials = [
    { icon: "🥇", label: "2016 Rio Olympian" },
    { icon: "🏅", label: "Oceania Gold Medallist" },
    { icon: "🏆", label: "Auckland's Top Competition Club" },
    { icon: "🥋", label: "WT Certified Coaching" },
    { icon: "⭐", label: "2nd Dan Black Belt Instructor" },
];
---

<div
    class="bg-deep-space-black/80 backdrop-blur-sm border-b border-white/10 py-3 overflow-hidden"
    aria-label="Trust credentials"
>
    <div
        class="flex items-center justify-center flex-wrap gap-x-8 gap-y-2 container mx-auto px-4"
    >
        {
            credentials.map((c) => (
                <div class="flex items-center gap-2 text-sm font-oswald uppercase tracking-wider text-gray-300 whitespace-nowrap">
                    <span class="text-base" aria-hidden="true">
                        {c.icon}
                    </span>
                    <span>{c.label}</span>
                </div>
            ))
        }
    </div>
</div>
